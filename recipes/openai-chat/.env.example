# OpenAI API Configuration
OPENAI_API_KEY=sk-your_api_key_here

# Optional: System prompt
SYSTEM_PROMPT="You are a helpful assistant".

# Optional: User prompt
PROMPT="Write a haiku about code execution in the cloud"

# Optional: Model selection (default: gpt-4o-mini)
# Available: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
MODEL=gpt-4o-mini

# Optional: Temperature (0-2, default: 0.7)
TEMPERATURE=0.7

# Optional: Max tokens (default: 200)
MAX_TOKENS=200
