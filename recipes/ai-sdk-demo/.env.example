# OpenAI API Configuration
OPENAI_API_KEY=sk-your_openai_api_key_here

# Optional: Customize the prompt
PROMPT=Explain the importance of fast language models in 3 sentences.

# Optional: Model selection (default: gpt-4o-mini)
# Available models: gpt-4o, gpt-4o-mini, gpt-5, gpt-5-mini, gpt-5-nano, o1, o1-mini
MODEL=gpt-4o-mini

# Optional: Temperature (0-2, default: 0.7)
# Lower values = more focused, higher values = more creative
TEMPERATURE=0.7

# Optional: Max tokens (default: 500)
MAX_TOKENS=500
